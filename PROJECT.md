# Project setup

The pinned requirements are committed so every contributor and CI run installs the same dependencies. The `.venv` directory remains local and ignored because virtual environments are machine-specific and should not be committed.